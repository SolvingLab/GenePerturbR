% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/visualization.R
\name{gpdb_plot_volcano}
\alias{gpdb_plot_volcano}
\title{Plot Volcano Plot for Single Dataset}
\usage{
gpdb_plot_volcano(
  dataset_id,
  padj_cutoff = 0.05,
  logfc_cutoff = 1,
  nlabel = 10,
  highlight_genes = NULL,
  colors = NULL,
  point.maxsize = 4,
  point.alpha = 0.8,
  intercept.width = 0.65,
  label.size = 3.5,
  label.bg = "white",
  label.bg.r = 0.1,
  legend.position = "bottom",
  title = NULL,
  theme.plot = NULL
)
}
\arguments{
\item{dataset_id}{Character. Dataset ID (e.g., "D10001"). Required.}

\item{padj_cutoff}{Numeric. Significance threshold (default 0.05)}

\item{logfc_cutoff}{Numeric. Fold change threshold in log2 scale (default 1 = 2-fold)}

\item{nlabel}{Integer. Number of top genes to label (default 10)}

\item{highlight_genes}{Character vector. Specific genes to highlight (optional)}

\item{colors}{Three colors for Up/NoSig/Down genes (default: blue/grey/red)}

\item{point.maxsize}{Maximum size of points (default 4)}

\item{point.alpha}{Alpha transparency of points (default 0.8)}

\item{intercept.width}{Width of threshold lines (default 0.65)}

\item{label.size}{Size of gene labels (default 3.5)}

\item{label.bg}{Background color for labels (default "white")}

\item{label.bg.r}{Background corner radius for labels (default 0.1)}

\item{legend.position}{Position of legend (default "bottom")}

\item{title}{Character. Plot title (auto-generated if NULL)}

\item{theme.plot}{ggplot2 theme (default theme_bw)}
}
\value{
ggplot object
}
\description{
Create volcano plot showing differential expression from ONE dataset.
Displays log2 fold change vs -log10(adjusted p-value).
}
\examples{
\dontrun{
# Basic volcano plot
p1 <- gpdb_plot_volcano("D10001")

# Highlight specific genes
p2 <- gpdb_plot_volcano("D10001", highlight_genes = c("MYC", "TP53", "KRAS"))

# Customize appearance
p3 <- gpdb_plot_volcano("D10001",
  padj_cutoff = 0.01,
  logfc_cutoff = 2,
  nlabel = 20
)
}
}
