% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/enrichment.R
\name{gpdb_plot_enrichment}
\alias{gpdb_plot_enrichment}
\title{Plot Paired Enrichment Dotplot}
\usage{
gpdb_plot_enrichment(
  enrich_result,
  show.term.num = 15,
  use.all = FALSE,
  x = "FoldEnrich",
  color.by = "p.adjust",
  size.by = "Count",
  colors = c("#003c30", "#01665e", "#35978f", "#80cdc1", "#c7eae5", "#f6e8c3", "#dfc27d",
    "#bf812d", "#8c510a", "#543005"),
  size.range = c(3, 8),
  title = NULL,
  up.title = "Upregulated Genes",
  down.title = "Downregulated Genes",
  legend.position = "right",
  theme = ggplot2::theme_bw(base_rect_size = 1.5)
)
}
\arguments{
\item{enrich_result}{A gpdb_enrichment object from gpdb_enrich()}

\item{show.term.num}{Integer. Number of top pathways to show per side (default 15)}

\item{use.all}{Logical. Use all results or only significant ones (default FALSE, use significant)}

\item{x}{Character. X-axis variable: "FoldEnrich", "pvalue", "p.adjust", "Count" (default "FoldEnrich")}

\item{color.by}{Character. Color variable: "p.adjust", "pvalue", "Count", "FoldEnrich" (default "p.adjust")}

\item{size.by}{Character. Size variable: "Count", "pvalue", "p.adjust", "FoldEnrich" (default "Count")}

\item{colors}{Character vector. Color palette (default: BrBG diverging palette)}

\item{size.range}{Numeric vector of length 2. Size range for points (default c(3, 8))}

\item{title}{Character. Main title (default: auto-generated)}

\item{up.title}{Character. Title for upregulated panel (default "Upregulated Genes")}

\item{down.title}{Character. Title for downregulated panel (default "Downregulated Genes")}

\item{legend.position}{Character. Legend position: "right", "bottom", "none" (default "right")}

\item{theme}{ggplot2 theme (default theme_bw)}
}
\value{
A patchwork object combining two dotplots
}
\description{
Create side-by-side dotplots showing enrichment results for upregulated
and downregulated genes. Inspired by GSEA paired plots.
}
\examples{
\dontrun{
# Basic usage
targets <- gpdb_find_targets("TP53", top_n = 100)
enrich_res <- gpdb_enrich(targets, enrich.type = "GO")
gpdb_plot_enrichment(enrich_res)

# Customize appearance
gpdb_plot_enrichment(
  enrich_res,
  show.term.num = 20,
  x = "pvalue",
  color.by = "FoldEnrich"
)
}
}
