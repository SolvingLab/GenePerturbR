% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/visualization.R
\name{gpdb_plot_heatmap_deg}
\alias{gpdb_plot_heatmap_deg}
\alias{gpdb_plot_heatmap}
\title{Plot DEG Heatmap Across Multiple Datasets}
\usage{
gpdb_plot_heatmap_deg(
  dataset_ids,
  genes = NULL,
  top_n = 50,
  cluster_rows = TRUE,
  cluster_cols = TRUE,
  scale = "row",
  colors = NULL,
  show_values = FALSE,
  title = NULL,
  theme = NULL
)

gpdb_plot_heatmap(
  dataset_ids,
  genes = NULL,
  top_n = 50,
  cluster_rows = TRUE,
  cluster_cols = TRUE,
  scale = "row",
  colors = NULL,
  show_values = FALSE,
  title = NULL,
  theme = NULL
)
}
\arguments{
\item{dataset_ids}{Character vector. Dataset IDs to compare (2+ required)}

\item{genes}{Character vector. Specific genes to show (optional)}

\item{top_n}{Integer. If genes not specified, show top N DEGs (default 50)}

\item{cluster_rows}{Logical. Cluster genes (default TRUE)}

\item{cluster_cols}{Logical. Cluster datasets (default TRUE)}

\item{scale}{Character. "row" (z-score), "column", or "none" (default "row")}

\item{colors}{Character vector. Color palette (NULL uses default spectral)}

\item{show_values}{Logical. Show values in cells (default FALSE)}

\item{title}{Character. Plot title}

\item{theme}{ggplot2 theme (NULL uses default)}
}
\value{
ggplot object
}
\description{
Compare differential expression (logFC) across multiple datasets.
Each column represents one dataset, each row represents one gene.
This function is designed for cross-dataset comparison of perturbation effects.
}
\examples{
\dontrun{
# Compare TP53 knockdown effects across datasets
datasets <- gpdb_list_datasets(gene = "TP53")
gpdb_plot_heatmap_deg(head(datasets$dataset_id, 5), top_n = 30)
}
}
\seealso{
\code{\link{gpdb_plot_heatmap_expr}} for single-dataset sample-level heatmaps
}
