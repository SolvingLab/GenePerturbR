% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/visualization.R
\name{gpdb_plot_heatmap_expr}
\alias{gpdb_plot_heatmap_expr}
\alias{gpdb_plot_heatmap_single}
\title{Plot Expression Heatmap for Single Dataset}
\usage{
gpdb_plot_heatmap_expr(
  dataset_id,
  top_up = 20,
  top_down = 20,
  scale = "row",
  cluster_rows = TRUE,
  cluster_cols = TRUE,
  show_gene_names = TRUE,
  colors = NULL,
  show_values = FALSE,
  title = NULL,
  theme = NULL
)

gpdb_plot_heatmap_single(
  dataset_id,
  top_up = 20,
  top_down = 20,
  scale = "row",
  cluster_rows = TRUE,
  cluster_cols = TRUE,
  show_gene_names = TRUE,
  colors = NULL,
  show_values = FALSE,
  title = NULL,
  theme = NULL
)
}
\arguments{
\item{dataset_id}{Character. Dataset ID to visualize}

\item{top_up}{Integer. Number of top upregulated genes (default 20)}

\item{top_down}{Integer. Number of top downregulated genes (default 20)}

\item{scale}{Character. "row" (z-score), "column", or "none" (default "row")}

\item{cluster_rows}{Logical. Cluster genes (default TRUE)}

\item{cluster_cols}{Logical. Cluster samples (default TRUE)}

\item{show_gene_names}{Logical. Show gene names (default TRUE)}

\item{colors}{Character vector. Color palette (NULL uses default)}

\item{show_values}{Logical. Show values in cells (default FALSE)}

\item{title}{Character. Plot title (auto-generated if NULL)}

\item{theme}{ggplot2 theme (NULL uses default)}
}
\value{
ggplot object
}
\description{
Visualize sample-level gene expression for one dataset.
Each column represents one sample, each row represents one gene.
Shows top upregulated and downregulated genes with sample group annotations.
}
\examples{
\dontrun{
# Plot sample-level expression heatmap
gpdb_plot_heatmap_expr("D10001", top_up = 20, top_down = 20)

# Customize number of genes
gpdb_plot_heatmap_expr("D10001", top_up = 15, top_down = 15)
}
}
\seealso{
\code{\link{gpdb_plot_heatmap_deg}} for multi-dataset DEG comparison
}
